from setuptools import setup, find_packages

setup(
    name="",
    version="0.1",
    packages=find_packages(where="src"),
    package_dir={"": "src"},
    author="Names",
    author_email="",
    description="Description",
    install_requires=[],
    entry_points={}
)